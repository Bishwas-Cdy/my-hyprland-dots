After reinstall + cloning your repo:

```sh
cd ~/my-hyprland-dots
./restore-dots.sh
```
---

Whenever you change configs:

```sh
cd ~/my-hyprland-dots
./update-dots.sh
```

and it’ll sync → commit → push in one go


